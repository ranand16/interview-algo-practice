// function tappingRainWater(array){
//     let last_max = 0, index = 0, tot_area = 0;
//     array.forEach((element, i) => {  
//         if(!last_max || element === array[i-1]) last_max = element
//         else if(element < last_max) tot_area += last_max - element;
//         else if(element >= last_max) {
//             last_max = element;

//         }
//         else last_max = 0;
//         console.log(element, last_max, tot_area)
//     });
//     return tot_area
// }
// 0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1

function tappingRainWater(array){
    
}